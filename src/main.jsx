import React from 'react'
import ReactDOM from 'react-dom/client'
import SeatingPlanner from './SeatingPlanner.jsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <SeatingPlanner />
  </React.StrictMode>,
)
